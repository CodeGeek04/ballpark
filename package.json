{
  "name": "hunch-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "ballpark": "pnpm --filter ballpark",
    "portal": "pnpm --filter portal",
    "toppl": "pnpm --filter toppl"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
